class Home extends React.Component{
    constructor(props){
        super(props);
        this.state={
            // shoppingList:this.props.model.getShoppingList(),
            // guest : this.props.model.getNumberOfGuests()
        }
        // this.summaryCallBack= ()=>this.setState({shoppingList:this.props.model.getShoppingList(), guest:this.props.model.getNumberOfGuests()});
    }
    render(){
        return  <div>hey there</div>
    }
}

class ArtworkInfo extends React.Component{

    constructor(props){
        super(props);
        this.state={
            // shoppingList:this.props.model.getShoppingList(),
            // guest : this.props.model.getNumberOfGuests()
        }
        // this.summaryCallBack= ()=>this.setState({shoppingList:this.props.model.getShoppingList(), guest:this.props.model.getNumberOfGuests()});
    }
    render(){
        return  <div>hey there</div>
    }
}

class ArtworkDescription extends React.Component{

    constructor(props){
        super(props);
        this.state={
            // shoppingList:this.props.model.getShoppingList(),
            // guest : this.props.model.getNumberOfGuests()
        }
        // this.summaryCallBack= ()=>this.setState({shoppingList:this.props.model.getShoppingList(), guest:this.props.model.getNumberOfGuests()});
    }
    render(){
        return  <div>hey there</div>
    }
}

class ArtworkInfo extends React.Component{

    constructor(props){
        super(props);
        this.state={
            // shoppingList:this.props.model.getShoppingList(),
            // guest : this.props.model.getNumberOfGuests()
        }
        // this.summaryCallBack= ()=>this.setState({shoppingList:this.props.model.getShoppingList(), guest:this.props.model.getNumberOfGuests()});
    }
    render(){
        return  <div>hey there</div>
    }
}

class UserCollectionTitle extends React.Component{

    constructor(props){
        super(props);
        this.state={
            // shoppingList:this.props.model.getShoppingList(),
            // guest : this.props.model.getNumberOfGuests()
        }
        // this.summaryCallBack= ()=>this.setState({shoppingList:this.props.model.getShoppingList(), guest:this.props.model.getNumberOfGuests()});
    }
    render(){
        return  <div>hey there</div>
    }
}

class UserCollectionContent extends React.Component{

    constructor(props){
        super(props);
        this.state={
            // shoppingList:this.props.model.getShoppingList(),
            // guest : this.props.model.getNumberOfGuests()
        }
        // this.summaryCallBack= ()=>this.setState({shoppingList:this.props.model.getShoppingList(), guest:this.props.model.getNumberOfGuests()});
    }
    render(){
        return  <div>hey there</div>
    }
}